---
title: Orbis 技术设计
doc_id: 04
type: tech-design
status: draft
version: v0.1
stage: MVP 技术方案 → 开发输入
parent: 02-MVP-PRD v0.3
upstream:
  - 00-产品基石 v0.6（§7 架构 / §8 安全 / §9 运维 / §12 规则与验收）
  - 01-MVP功能清单 v0.4（P0 = 范围契约）
  - 02-MVP-PRD v0.3（Given/When/Then 验收）
  - pm/Phase0-冲刺A-调研记录.md（License / F4 上游）
  - pm/Phase0-冲刺B-调研记录.md（E1 版本源 / 鸣潮 Provider 声明）
  - pm/Phase0-冲刺C-F2种子表定稿.md（兼容性种子表）
platform: Windows PC 首发
updated: 2026-09-18
aliases:
  - 技术设计
  - Tech Design
tags:
  - tech
  - design
  - mvp
---

# Orbis 技术设计文档（MVP）

> **Technical Design / HOW 文档**
>
> - 定位：回答「怎么实现」；范围以 [01 清单](01-MVP功能清单.md) P0 为墙，不突破 [02 PRD](02-MVP-PRD.md) 约定；实体与分层沿用 [00 基石](00-产品基石.md) §7，不自创体系
> - 证据锚点：所有 Phase 0 调研结论以 `P0-A / P0-B / P0-C §x` 引用，不重复论证
> - 落地节奏：对齐 01 §6 开发顺序（地基 → 广度 → 深度鸣潮 → 深度原神 → UI → E1）

---

## 目录

1. [设计输入与原则](#1-设计输入与原则)
2. [技术栈定稿](#2-技术栈定稿)
3. [进程与运行时架构](#3-进程与运行时架构)
4. [代码架构：Workspace 与分层](#4-代码架构workspace-与分层)
5. [核心模块设计](#5-核心模块设计)
6. [数据设计](#6-数据设计)
7. [安全模型落地（L0–L3）](#7-安全模型落地l0l3)
8. [降级与错误策略](#8-降级与错误策略)
9. [测试与架构不变量保障](#9-测试与架构不变量保障)
10. [待实测清单（首批工程验证项）](#10-待实测清单首批工程验证项)
11. [开放问题](#11-开放问题)
- [附录 A：模块 ↔ 功能覆盖矩阵](#附录-a模块--功能覆盖矩阵)
- [附录 B：变更记录](#附录-b变更记录)

---

## 1. 设计输入与原则

### 1.1 Phase 0 证据 → 本文消费点

| 证据 | 结论摘要 | 消费章节 |
|------|----------|----------|
| P0-A §1 | F4 上游 = 34736384/genshin-fps-unlock（MIT，v3.0.4），sefinek fork 备选 | §5.8 |
| P0-A §2 | 项目 License = GPL-3.0-or-later；MIT 系可自由借鉴 | §2.2 |
| P0-A §1.3 | L3 口径 = 默认关闭 + 显式授权 + 发行包分离 | §7.2 |
| P0-B §1–§4 | E1：米哈游系 HoYoPlay `getGamePackages` + 鸣潮 CDN `index.json` 直连；终末地降级 | §5.2 |
| P0-B §5 | VersionProvider 接口含 region 维度；三 adapter；待实测清单 | §5.2 / §10 |
| P0-B §6 | 鸣潮键级声明（双键 upsert）+ 五条工程红线 + 整目录备份 | §5.3 / §5.4 / §7.1 |
| P0-C §1–§3 | 种子表 schema 0.1.0；exact → prefix → Unknown 匹配；首批 3 条 | §5.6 / §6.2 |
| P0-C §4.1 | 种子表 repo 路径 `data/compatibility/seed.json`，随发行包内置、只读加载 | §6.2 |

### 1.2 00 §12.3 十条规则 → 工程手段

| # | 规则（00 §12.3） | 工程手段 | 章节 |
|---|------------------|----------|------|
| 1 | Game Core 不硬编码游戏逻辑 | Cargo Workspace 依赖方向 + catalog 注册模式 + CI grep 断言 | §4.1 |
| 2 | 所有游戏通过 Game Provider 接入 | 每游戏一个装配条目（`GameProvider` struct） | §4.2 |
| 3 | 所有增强通过 Tool Provider 接入 | Tool Manifest 数据驱动 + 执行器分派 | §5.7 |
| 4 | Tool 必须有版本和兼容性 | manifest `version` + 兼容性权威 = seed.json | §5.6 / §5.7 |
| 5 | 修改配置前必须 Backup | EnhancementExecutor 顺序约束（无备份不修改） | §5.5 |
| 6 | 修改失败必须能 Rollback | L1 状态机 RollingBack 分支 + 哈希校验恢复 | §5.5 |
| 7 | 高风险操作显式授权 | L3 授权记录持久化（文案哈希 + 时间） | §7.2 |
| 8 | 第三方项目记录 License/Source/Author | manifest `source` 字段 + NOTICE 义务 | §2.2 / §5.7 |
| 9 | Launcher 不依赖任何单一 Tool | Tool 失败一律降级不阻塞主流程 | §8 |
| 10 | UI 不暴露底层复杂性 | IPC 命令面只暴露语义动词（00 §7.9） | §4.3 |

### 1.3 刻意不做（防过度设计）

- **动态插件加载**：MVP 工具全部内置，Manifest 是数据声明而非热加载机制（`orbis-builtin/*` / `orbis-bundled/*` 命名空间已预留外部化，P0-C §1.2）
- **应用自更新**：MVP 不做，GitHub Releases 手动下载
- **i18n 框架**：MVP 中文单语；文案集中管理 + 轻量 `t()` 封装，框架 P1+
- **诊断中心 / 配置差异**：P1（01 §4）
- **下载引擎 / 账号 / 云同步**：不做（01 §4）

---

## 2. 技术栈定稿

### 2.1 选型总表

| 层 | 选型 | 依据 |
|----|------|------|
| 桌面运行时 | Tauri 2.x（跟随 stable） | 00 §7.7；Twintail 同栈先例 |
| Core | Rust（stable edition） | 00 §7.7 Native Core 定位 |
| UI | React 18 + TypeScript + Vite | 00 §7.7 |
| UI 服务端状态 | TanStack Query | IPC 查询缓存 / 失效刷新（E1、A5 轮询） |
| UI 本地状态 | Zustand | 轻量，避免状态库过度设计 |
| 样式 | CSS Variables + 设计 token | 对齐 [03 UI](03-UI设计文档.md) §2 设计系统（zero_hue_cyber_minimal） |
| 数据库 | SQLite（`rusqlite` bundled） | 00 §7.7；鸣潮 LocalStorage.db 同为 SQLite，读写经验复用 |
| HTTP | `reqwest` + rustls | 版本源只读 GET（P0-B §0 判定标准） |
| 进程 / 注册表 | `sysinfo` + `winreg` | A5 运行状态 / A1 发现 |
| 哈希 | `sha2` | 备份完整性 / 解锁器资产校验 |
| 错误 | `thiserror`（crate 内）/ `anyhow`（应用层） | — |
| 日志 | `tracing` + 自定义 JSONL Layer | 00 §9.1 schema 直出 |
| IPC 类型绑定 | `tauri-specta`（建议引入） | Rust ↔ TS 类型单源，避免手工双维护 |
| 打包 | Tauri bundler（NSIS 优先） | R4 无签名兜底 = 白名单引导文档（02 §6） |

### 2.2 依赖治理（00 §8.6 义务）

- 全部核心依赖为 MIT / Apache-2.0 / MIT-3-Clause 系，与 GPL-3.0-or-later 兼容（P0-A §2 决策面不受影响）
- CI 引入 `cargo deny check`（License / 重复依赖 / 安全公告）
- 发布 checklist 含 NOTICE 文件生成（第三方组件 + 上游工具声明，02 §5「开源合规」行）
- GPL 系传染性红线：**GPL 工具只作外部进程调用，不嵌入、不静态链接**（00 §8.6）；F4 上游为 MIT，无此约束

---

## 3. 进程与运行时架构

### 3.1 进程拓扑

```
┌───────────────────────────────── Windows ─────────────────────────────────┐
│                                                                            │
│  ┌────────────────┐    Tauri IPC     ┌─────────────────────────────────┐  │
│  │  WebView (UI)  │◄────────────────►│  Orbis 主进程（Rust Core）       │  │
│  │  React + TS    │   命令 + 事件    │  Workspace crates 组装           │  │
│  │  S0–S6 页面    │                  │  默认普通用户权限（00 §8.9）     │  │
│  └────────────────┘                  └────────┬───────────────┬────────┘  │
│                                               │ spawn         │ spawn    │
│                                     ┌─────────▼──────┐ ┌─────▼─────────┐ │
│                                     │ 游戏进程        │ │ 解锁器子进程   │ │
│                                     │（5 款游戏 exe）│ │（B7 独立资产） │ │
│                                     └────────────────┘ └───────────────┘ │
│                                                                            │
│   出网（只读 / 低频 / 用户可关，P0-B §5.4）：                                │
│   HoYoPlay API · 库洛 CDN index.json · GitHub Releases（解锁器资产）        │
└────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 职责边界

| 进程 | 职责 | 禁止 |
|------|------|------|
| WebView UI | 呈现 + 交互意图表达（S0–S6，03 §3） | 不接触路径拼接 / SQL / 进程句柄 / 哈希（00 §7.9） |
| Orbis 主进程 | 全部决策：扫描、版本比对、门控、备份、写入、编排、日志 | 不依赖任何单一 Tool 存活（00 原则 02） |
| 解锁器子进程 | 原神运行时解锁（上游 CLI 模式） | 无 UI 依赖；退出不拖垮主进程 |
| 游戏进程 | 不感知 Orbis | Orbis 不注入非 L3 授权进程 |

### 3.3 权限模型

- **默认普通用户权限运行**（00 §8.9），不申请永久管理员
- 写入目标权限不足（如鸣潮装于 `Program Files`）→ precheck 失败 → 阻止 + 引导（§7.1）；受限提权方案为开放问题（§11 Q2）

---

## 4. 代码架构：Workspace 与分层

### 4.1 Workspace 与依赖方向

```
orbis/
├── pm/                              # 产品文档（既有）
├── data/
│   ├── compatibility/seed.json      # 兼容性种子表（P0-C §4.1）
│   └── tools/
│       ├── manifests/*.json         # 内置工具 Manifest（C1）
│       └── assets.json              # 解锁器发行资产清单（URL + SHA256，B7）
├── crates/
│   ├── orbis-core/                  # 领域模型 + 能力 trait + 兼容引擎（零游戏知识）
│   ├── orbis-platform/              # SQLite / 进程 / 注册表 / 日志 / 备份（OS 落地）
│   ├── orbis-providers/             # 5 游戏装配 + 3 版本 adapter + 鸣潮 ConfigProvider + 扫描规则
│   └── orbis-tools/                 # Manifest 加载 + L1/L3 执行器 + 解锁器资产
├── src-tauri/                       # Tauri 壳：命令注册 / 生命周期 / 事件桥（唯一组装点）
└── src/                             # React UI（S0–S6）
```

依赖规则（编译期保证架构不变量，详见 §9.1）：

```
providers → core, platform
tools     → core, platform
platform  → core
src-tauri → providers, tools, platform, core      （唯一允许依赖全部的组装根）
禁止：providers ↔ tools 横向依赖；core 依赖任何上游 crate
```

### 4.2 GameProvider 装配模型（两级结构）

00 §7.2 的完整 `GameProvider`（install/update/repair/account）是长期形态；MVP 按 P0-B §5 拆为**按能力横切的 trait** + **每游戏一个装配条目**：

```rust
// orbis-core —— 能力 trait（签名级伪代码）

pub trait VersionSource: Send + Sync {
    /// region 从第一天进入签名（P0-B §5.1，Q7）
    async fn remote_version(&self, game: GameId, region: Region)
        -> Result<RemoteVersion, VersionError>;
}

pub trait ConfigSource: Send + Sync {
    /// A8 范围依据：声明式备份范围（未声明 = UI「暂不支持备份」）
    fn declared_paths(&self, install: &GameInstallation) -> Vec<PathBuf>;
    /// 前置检查：进程未运行 + 写权限（P0-B §6.3 红线 1）
    fn precheck(&self, install: &GameInstallation) -> Result<(), PrecheckError>;
    fn read_keys(&self, install: &GameInstallation, keys: &[ConfigKey])
        -> Result<Vec<ConfigValue>, ConfigError>;
    /// 幂等 upsert 写入（§5.3）
    fn apply(&self, install: &GameInstallation, plan: &[ConfigChange]) -> Result<(), ConfigError>;
}

pub trait LaunchSpec: Send + Sync {
    fn executable(&self, install: &GameInstallation) -> PathBuf;
    fn default_args(&self, region: Region) -> Vec<String>;   // 待实测 T5
}

pub trait DetectRule: Send + Sync {
    fn detect(&self) -> Vec<GameInstallationCandidate>;       // 只读，L0
}

/// 装配根：每个游戏一个条目（00 §7.1「每个游戏都是一个 Provider」）
pub struct GameProvider {
    pub game: GameStaticInfo,                    // id / name / publisher / icon
    pub detect: Box<dyn DetectRule>,
    pub version: Option<Box<dyn VersionSource>>, // None = 降级展示
    pub config: Option<Box<dyn ConfigSource>>,   // None = 「暂不支持备份」（A8）
    pub launch: Box<dyn LaunchSpec>,
}
```

MVP 装配表（5 款）：

| GameId（P0-C §1.2 slug） | VersionSource | ConfigSource | 状态说明 |
|--------------------------|---------------|--------------|----------|
| `genshin-impact` | `HoyoPlayAdapter` | —（L3 不落盘，A8 不适用） | E1 直连 |
| `honkai-star-rail` | `HoyoPlayAdapter` | — | E1 直连 |
| `zenless-zone-zero` | `HoyoPlayAdapter` | — | E1 直连 |
| `wuthering-waves` | `KuroAdapter` | `WuwaConfigProvider` | L1 深度标杆 |
| `arknights-endfield` | `DegradedAdapter` | — | E1 降级（P0-B §3） |

`Region = { Cn, Global, Bili }`（Q7；Bili 版本内容随国服，版本源复用 Cn）。

### 4.3 IPC 命令面（00 §7.9 UI 解耦落地）

UI 只见语义动词。命令（camelCase）：

| 命令 | 说明 | 功能 |
|------|------|------|
| `scanGames` / `listInstallations` / `addInstallation` / `removeInstallation` / `getInstallationDetail` | 扫描（含进度事件）与安装实例管理 | A1–A3 |
| `launchGame(id, { skipUnlock })` / `terminateGame(id)` | 启动编排 / 结束进程（风险提示前置） | A4 / B8 |
| `refreshRemoteVersions` / `getUpdateStatus` | 只读版本探测（启动时 + 手动 + 可关） | E1 |
| `listTools(gameId?)` / `getTool(toolId)` / `setToolEnabled` / `grantToolAuthorization` | 工具查询 / 启停 / L3 授权 | C1–C3 / B7 |
| `createBackup` / `listBackups` / `restoreBackup` | 备份与恢复 | A8 / B5 |
| `getCompatibility(gameId, toolId)` | 兼容状态查询 | C4 / B6 |
| `getPlaytime(scope)` | 时长统计 | A6 |

事件（Core → UI）：`scan:progress`、`game:state-changed`、`tool:apply-step`（D4 透明卡片逐步驱动）、`version:refreshed`。

---

## 5. 核心模块设计

### 5.1 GameScanner（A1 / A2 / A3）

**发现规则（每游戏 `DetectRule`，严格只读）：**

| 游戏 | 规则来源 |
|------|----------|
| 米哈游系 ×3 | 注册表安装键（键名清单随 T7 实测定稿）+ 常见路径 + `getGameConfigs` 的 `installation_dir` 在线提示（可选辅助，P0-B §1.1） |
| 鸣潮 | `%APPDATA%\KRLauncher\*\*\kr_starter_game.json` 的 `path` 字段（P0-B §6.1）+ 常见路径 |
| 终末地 | `C:\Program Files\GRYPHLINK\` 布局 + 常见路径 |
| 兜底 | 各盘符标准目录探测（**不做全盘扫描**，NFR ≤ 3s，02 §5） |

**校验与识别：**

- 候选必须通过 exe 文件特征校验（防「官方启动器当游戏本体」，02 A2 边界）
- 本地版本识别顺序：exe `VERSIONINFO` → 特征文件 / 目录名 → `Unknown`（不猜、不显示过期缓存，02 A3）；识别方式记入 `version_source`
- 扫描超时 / 权限不足 → 部分结果 + 手动添加引导，不阻塞启动（02 A1）

### 5.2 VersionProvider ×3（E1，P0-B §4 / §5）

```rust
pub struct RemoteVersion {
    pub version: String,          // "3.5" / "7.0"
    pub fetched_at: DateTimeUtc,
    pub source: &'static str,     // "hyp:getGamePackages" / "kuro:index.json" / "degraded"
}
```

| Adapter | 输入 | 输出与要点 |
|---------|------|-----------|
| `HoyoPlayAdapter` | 一次 `GET getGamePackages`（`launcher_id=VYTpXlbWo8`，game_id 参数实际被忽略） | 按 game_id 过滤 `data.game_packages[].main.major.version`；**1 对 3 复用**；region → base 切换（国服 base 待实测 T1；备用 = 国服 mdk content API，P0-B §1.2） |
| `KuroAdapter` | `GET <CDN_BASE>/<appId>_<appKey>/G153/index.json` | `default.config.version`；**端点漂移兜底**：`KRApp.conf`（`base64(XOR(data, 0x63))`）解密恢复常量（P0-B §2.2）；国服常量差异待实测 T2 |
| `DegradedAdapter` | —（终末地） | 恒 `Unknown` + 官方渠道引导（GRYPHLINK 内更新），UI 标注「调研中」（P0-B §3.2） |

**调度与合规（P0-B §5.4）：**

- 触发：应用启动（延迟数秒，不阻塞冷启动 ≤ 3s）+ 手动刷新；设置项可关
- 失败 → `Unknown`，不误报（02 E1 验收）；结果会话内缓存
- 版本比较：自实现**数值点分比较器**（游戏版本非 semver，如 `3.5` / `7.0`），本地 < 远程 → `UpdateAvailable`

### 5.3 WuwaConfigProvider（B1，P0-B §6 键级声明）

**目标文件：** `<游戏根目录>\Wuthering Waves Game\Client\Saved\LocalStorage\LocalStorage.db`（SQLite，单表 `LocalStorage(key, value)`，value 为混合类型：整数或 JSON 文本，按键声明解析）。

**键声明（MVP 全集）：**

| 键 | 类型 | 写入语义 |
|----|------|----------|
| `CustomFrameRate` | INTEGER | 幂等 upsert 直写数值（2.2+ 语义；1.0 枚举索引语义仅存种子表 notes，P0-C §1.3） |
| `GameQualitySetting` | JSON | 读 → 反序列化 → 改 `KeyCustomFrameRate` → 写回（`rusqlite` 等价 FastChen 路径） |

```sql
-- rusqlite 预编译语句（幂等 upsert，P0-B §6.2）
INSERT INTO LocalStorage(key, value) VALUES ('CustomFrameRate', ?1)
ON CONFLICT(key) DO UPDATE SET value = ?1;
```

**红线落地（5 条 → 机制，P0-B §6.3）：**

| 红线 | 机制 | 章节 |
|------|------|------|
| 运行时不可写 | `precheck`：游戏进程（`Client-Win64-Shipping.exe`）+ KRLauncher 进程 + 写权限三查，失败阻止 | §5.5 |
| 游戏内改画质锁回 60 | 启用成功后一次性提示 + 工具详情常驻说明（B1 提示义务） | §7.1 |
| 上限 120 | UI 滑杆锁定 120 + `apply` 拒绝 > 120（更高值触发游戏修复机制） | §7.1 |
| db2 回写对抗 | MVP 交付基础双键写入；`MenuData` / `PlayMenuInfo` 重置 + TRIGGER 方案为 3.x 首测后的增强项 | §10 T3 |
| 验证时效 | 种子表 `Unknown` → B6 门控拦截 | §5.6 |

### 5.4 BackupManager（A8 / B2 / B4）

**备份单元 = 整目录快照**（P0-B §6.1：journal / db2 伴生文件必须覆盖，不能只拷 db）：

```rust
pub struct BackupRecord {
    pub id: BackupId,
    pub installation_id: InstallationId,
    pub tool_id: Option<ToolId>,           // None = 手动备份
    pub trigger: Trigger,                  // Manual | PreModify | PreRestore
    pub files: Vec<FileHash>,              // rel_path / sha256 / size
    pub created_at: DateTimeUtc,
}
```

- **create**：precheck（进程）→ 复制 `declared_paths` 展开的整目录 → 逐文件 SHA256 → 写 `manifest.json` + 落库
- **restore**：precheck → 先做「恢复前备份」（`PreRestore`，任何恢复本身可撤销，02 A8）→ 覆盖 → 逐文件哈希校验
- 存储：`backups/<installation_id>/<backup_id>/`（§6.3）；MVP 全量保留 + 手动清理

### 5.5 EnhancementExecutor：L1 链路状态机（B1–B4）

```
enableTool(wuwa-fps-120)
   │
   ▼
[Gate] 兼容门控：seed 查询 ∉ {Verified, Compatible} → Blocked（B6，解释原因）
   │
   ▼
[Precheck] 进程 / 写权限（红线 1）
   │
   ▼
[Backup] BackupManager::create(PreModify)     —— B2：无备份不修改（硬性顺序）
   │
   ▼
[Modify] ConfigSource::apply（双键 = 120）      —— B1
   │
   ▼
[Verify] read_back(双键) == 期望                —— B3
   │                    │
   ok                   fail
   │                    ▼
[Applied]          [RollingBack] restore(最新备份) + 哈希校验 —— B4
   │                    │
   ▼                    ▼
收尾事件 + 日志      [RolledBack] 明示「修改失败，已自动恢复原配置」
```

- 状态枚举：`Pending → GatePassed → BackedUp → Modified → Verified | RollingBack → RolledBack`
- **每步** = 一次 `tool:apply-step` 事件（驱动 D4 透明卡片）+ 一条结构化日志（00 §9.1，Category = Backup / Modify / Verify / Rollback）
- 执行器内 `catch_unwind`：工具链路任何 panic 转入 RollingBack，**不影响主进程**（00 原则 02 / 12.4 A「Tool 失败不影响 Launcher」）

### 5.6 CompatibilityEngine（C4 / B6，P0-C）

```rust
pub enum CompatStatus { Verified, Compatible, Unknown, Incompatible, Deprecated }

/// 匹配规则（P0-C §2.2，表驱动测试用例）
pub fn query(seed: &SeedTable, game: GameId, tool: ToolId, local_version: &str)
    -> CompatStatus
// 1. exact 匹配命中 → 返回该状态
// 2. prefix 匹配命中（"3.x" 覆盖 3.0–3.9）→ 返回该状态
// 3. 无命中 / 本地版本 Unknown / 种子表缺失 → Unknown（默认安全态）
```

- 数据源：`data/compatibility/seed.json`（schema 0.1.0，首批 3 条，P0-C §3）
- **权威关系**：兼容性唯一权威 = seed.json；Tool Manifest 的 `compatibility` 字段仅作概览展示，避免双源冲突
- 状态升级路径（P0-C §2.3）：MVP 由维护者手工更新文件，随发行包分发；鸣潮 3.x 首测后 `Unknown → Verified` 即数据生产机制首个循环（00 §11.2）

### 5.7 ToolManifest 与 ToolRuntime（C1 / C2 / C3）

**Manifest schema（00 §7.4 对齐 + 定稿扩展）：**

```json
{
  "id": "orbis-builtin/wuwa-fps-120",
  "game": "wuthering-waves",
  "name": "120 FPS 解锁",
  "version": "0.1.0",
  "type": "config_modify",
  "risk_level": "L1",
  "permissions": ["read_config", "write_config"],
  "requires_admin": false,
  "backup_required": true,
  "entry": { "executor": "wuwa_fps" },
  "source": { "kind": "builtin" },
  "compatibility_ref": "seed.json"
}
```

```json
{
  "id": "orbis-bundled/genshin-fps-unlock",
  "game": "genshin-impact",
  "name": "120 FPS 解锁（运行时）",
  "version": "0.1.0",
  "type": "external_process",
  "risk_level": "L3",
  "permissions": ["launch_external", "process_attach"],
  "requires_admin": false,
  "backup_required": false,
  "entry": { "executor": "unlocker", "asset": "genshin-fps-unlock", "channel": "releases" },
  "source": { "kind": "upstream", "repo": "34736384/genshin-fps-unlock",
              "license": "MIT", "version": "v3.0.4" }
}
```

- **加载**：`data/tools/manifests/*.json` 经 `include_str!` 打包进二进制；启动时解析 + `schema` 校验；单个损坏 → 该工具不可见，不影响其他工具（C1 验收）
- **ToolRuntime**：执行器分派表（`config_modify` → EnhancementExecutor；`external_process` → UnlockerRunner）；启停状态持久化 `tool_state` 表；C2 详情页展示 manifest 全量信息（含 License 来源，00 §8.6）
- **新增工具路径**：新 manifest +（仅当需要新执行器类型时）providers 扩展 —— Core 不动（架构不变量 B2，§9.1）

### 5.8 UnlockerRunner（B7，L3，发行包分离）

**首次启用流程（P0-A §1.3 口径）：**

```
风险告知（版本化文案，全屏确认）
   → 用户显式勾选确认
   → 从 Orbis Releases 独立资产下载（主安装包不含注入器）
   → SHA256 校验（清单 = data/tools/assets.json，随 Orbis 版本分发）
   → 解包至 appdata/tools/genshin-fps-unlock/<version>/
   → tool_state 记录 { asset_version, sha256, granted_at, consent_text_hash }
```

**运行时（与 §5.9 编排协作）：**

- 门控：`compat ∈ {Verified, Compatible}` 且本地版本已知，否则阻止并解释（B7 验收）
- 资产哈希与记录不符 → 拒绝加载（防篡改）
- 解锁器进程失败 / 崩溃 → **降级普通启动 + 明示「本次未解锁」**，绝不阻塞游戏本体（02 B7 边界）
- 上游：34736384/genshin-fps-unlock（MIT，v3.0.4，P0-A §1）；sefinek fork 备选；以外部子进程调用集成，实现可替换（00 §5.2）
- 资产构建管道（上游源码构建 vs 复用上游发行物）见开放问题 Q1

### 5.9 LauncherOrchestrator（A4 / B8）

```
launchGame(installation, opts { skipUnlock })
  1. 状态检查：已在运行 → 拒绝重复启动（A4）
  2. 原神 && 解锁启用 && !skipUnlock：
       兼容门控失败 → 阻止并解释（B7）
  3. spawn 游戏（LaunchSpec：exe + args(region) + cwd）   —— args 待实测 T5
  4. （原神）spawn 解锁器子进程                            —— 编排顺序待实测 T4
  5. ProcessMonitor 开始跟踪；PlaytimeSession 开始计时
```

- `skipUnlock` = 「本次不解锁」开关：正常启动且**不改变**工具启用状态（B8 验收）
- `terminateGame`：前置数据损坏风险提示 + 确认 + 日志（A4 边界）

### 5.10 ProcessMonitor 与 PlaytimeTracker（A5 / A6）

- `sysinfo` 进程快照轮询（5s 周期，满足 A5「5 秒内反映」）；匹配 = exe 路径前缀
- 游戏时长口径 = **游戏进程存活时长**（02 A6 边界，UI 明示）
- `playtime_session` 每 30s checkpoint 落库 → Orbis 崩溃 / 被杀，重启后时长不丢（A6 验收）
- 进程崩溃 → 状态回落 `Installed` + 写日志（A5）

### 5.11 LogStore（D3）

- `tracing` + 自定义 Layer → JSONL 按日滚动：`logs/orbis.YYYY-MM-DD.log`
- 行结构 = 00 §9.1 schema：`{ ts, source, category, level, message, context, related_game }`
- Source 枚举：`Game / Tool / Launcher / Backup / Update`；Category 枚举：`Detect / Launch / Backup / Modify / Verify / Rollback / Version / Auth`
- 保留 14 天（启动时清理，默认值待确认 §11 Q4）；仅本地，无遥测；D6 查看页 P1（文件可直接打开）

---

## 6. 数据设计

### 6.1 SQLite Schema（MVP 子集）

00 §7.10 十二实体中，MVP 落地 7 个（`Game` 为编译期静态 catalog 不入库；`GameVersion` 实时探测不入库；`GameAccount` / `DownloadTask` / `DiagnosticLog` / `ToolVersion` 均不在 MVP）：

```sql
PRAGMA user_version = 1;

CREATE TABLE installation (
  id              TEXT PRIMARY KEY,        -- UUID v4
  game_id         TEXT NOT NULL,           -- P0-C §1.2 slug
  region          TEXT NOT NULL,           -- cn | global | bili
  install_path    TEXT NOT NULL,
  executable_path TEXT NOT NULL UNIQUE,
  local_version   TEXT,                    -- NULL = Unknown（A3：不猜）
  version_source  TEXT,                    -- exe_versioninfo | feature_file | directory
  status          TEXT NOT NULL DEFAULT 'installed',
  added_via       TEXT NOT NULL,           -- scan | manual
  created_at      INTEGER NOT NULL,
  updated_at      INTEGER NOT NULL
);

CREATE TABLE launch_profile (              -- A7：参数按安装实例独立保存
  installation_id TEXT PRIMARY KEY REFERENCES installation(id) ON DELETE CASCADE,
  args            TEXT NOT NULL DEFAULT '',
  env_json        TEXT NOT NULL DEFAULT '{}',
  updated_at      INTEGER NOT NULL
);

CREATE TABLE playtime_session (            -- A6
  id              TEXT PRIMARY KEY,
  installation_id TEXT NOT NULL REFERENCES installation(id) ON DELETE CASCADE,
  started_at      INTEGER NOT NULL,
  checkpoint_at   INTEGER NOT NULL,        -- 崩溃恢复依据
  ended_at        INTEGER,
  duration_sec    INTEGER NOT NULL DEFAULT 0
);
CREATE INDEX idx_session_install ON playtime_session(installation_id, started_at);

CREATE TABLE tool_state (                  -- C2 / B7
  tool_id            TEXT PRIMARY KEY,     -- namespace/name（P0-C）
  enabled            INTEGER NOT NULL DEFAULT 0,
  authorization_json TEXT,                 -- L3：{ granted_at, consent_text_hash, tool_version }
  asset_json         TEXT,                 -- 解锁器资产：{ version, path, sha256 }
  updated_at         INTEGER NOT NULL
);

CREATE TABLE backup (                      -- 00 §7.10 Backup 实体
  id              TEXT PRIMARY KEY,
  installation_id TEXT NOT NULL REFERENCES installation(id) ON DELETE CASCADE,
  game_id         TEXT NOT NULL,
  tool_id         TEXT,                    -- NULL = 手动备份
  trigger         TEXT NOT NULL,           -- manual | pre_modify | pre_restore
  file_count      INTEGER NOT NULL,
  total_bytes     INTEGER NOT NULL,
  manifest_json   TEXT NOT NULL,           -- files[]: { rel_path, sha256, size }
  created_at      INTEGER NOT NULL
);
CREATE INDEX idx_backup_install ON backup(installation_id, created_at DESC);

CREATE TABLE app_setting (
  key   TEXT PRIMARY KEY,
  value TEXT NOT NULL                      -- JSON
);
-- 预置键：version_check.enabled / log.retention_days / playtime.checkpoint_sec
```

### 6.2 内置数据（种子表 / Manifest / 资产清单）

| 数据 | repo 路径 | 打包 | 运行时行为 |
|------|-----------|------|-----------|
| 兼容性种子表 | `data/compatibility/seed.json` | `include_str!` | 启动校验 `schema_version`；不符或缺失 → 全部 `Unknown`（C4 验收），不崩溃 |
| 工具 Manifest | `data/tools/manifests/*.json` | `include_str!` | 单个损坏 → 该工具不可见（C1 验收） |
| 解锁器资产清单 | `data/tools/assets.json` | `include_str!` | URL + SHA256；哈希不符拒绝加载 |

种子表内容不复制进 SQLite（只读内存索引，量级 < 100 条）；MVP 无在线更新（P0-C §4.2）。

### 6.3 应用数据目录布局

```
%APPDATA%\orbis\
├── orbis.db                              # SQLite（WAL 模式）
├── backups\<installation_id>\<backup_id>\
│   ├── manifest.json
│   └── files\…                           # 整目录快照
├── tools\genshin-fps-unlock\<version>\…  # 发行包分离的解锁器资产
└── logs\orbis.YYYY-MM-DD.log             # JSONL，保留 14 天
```

---

## 7. 安全模型落地（L0–L3）

### 7.1 L1（鸣潮 120 FPS）执行红线汇总

P0-B §6.3 五条红线全部映射到机制（§5.3 表）；补充两条 UI 义务：

- **启用成功提示**：「游戏内修改画质会将帧率锁回 60，需重新启用工具」（红线 2 的文案义务）
- **Advanced 视图**（00 §12.2）：透明卡片可展开底层细节 —— `Modified: LocalStorage.db · Changed: KeyCustomFrameRate · Old: 60 → New: 120`

### 7.2 L3（原神解锁）授权与门控

| 环节 | 机制 |
|------|------|
| 默认状态 | `enabled = false`（C3 验收） |
| 首次启用 | 版本化风险文案（进程级修改说明 / 社区历史情况 / 不承诺绝对安全，02 B7）→ 显式勾选 → `authorization_json` 记录文案哈希与时间 |
| 持续门控 | 每次启动编排重新查询 seed（版本更新 → Unknown → 阻止 + 行动建议） |
| 资产完整性 | SHA256 双重校验（下载时 + 每次加载时） |
| 失败降级 | 解锁器任何失败 → 普通启动 + 「本次未解锁」（§5.8） |
| 风险口径 | 「社区多年实践、非官方授权」统一文案（P0-B §6.4） |

### 7.3 L0 / L2 边界（MVP 内不涉新机制）

- L0（扫描 / 版本 / 时长）：严格只读，不碰进程内存（02 A1 边界）
- L2（启动参数 A7）：参数持久化 + 实际生效，MVP 不做参数冲突检测（02 A7 边界）

---

## 8. 降级与错误策略

| 场景 | 行为 | 依据 |
|------|------|------|
| 版本源不可达 / 超时 | `Unknown`，不误报 | 02 E1 验收 |
| 终末地 E1 | 本地版本展示 + 官方渠道引导，标「调研中」 | P0-B §3.2 |
| 种子表缺失 / 损坏 | 兼容查询全部 `Unknown` | 02 C4 验收 |
| 单个 Manifest 损坏 | 该工具不可见，其余正常 | 02 C1 验收 |
| 解锁器下载 / 加载失败 | 阻止启用或降级普通启动 + 明示 | 02 B7 边界 |
| 写权限不足 | precheck 阻止 + 引导 | 02 A8 / B1 |
| 备份失败 | 不进入 Modify（顺序约束） | 02 B2 |
| 工具链路 panic | catch_unwind → 回滚 → 主流程不受影响 | 00 原则 02 |
| 扫描超时 / 权限不足 | 部分结果 + 手动添加引导 | 02 A1 |
| 版本无法识别 | `Unknown`，不显示过期缓存 | 02 A3 |

**总原则：任何降级必须显式可见，禁止静默失败（00 §8.3 不做隐式操作）。**

---

## 9. 测试与架构不变量保障

### 9.1 架构不变量（00 §12.4 B 组）的工程保障

| 不变量 | 保障手段 |
|--------|----------|
| 新增游戏不需要修改 Core | ① 依赖方向：`orbis-core` 不依赖任何含游戏知识的 crate（编译期保证）② catalog 注册模式：新游戏 = providers 加模块 + catalog 条目 + seed 数据 ③ CI grep 断言：5 个 game slug 不得出现在 `crates/orbis-core` |
| 新增 Tool 不需要修改 Core | ① 执行器分派表（两类执行器 `config_modify` / `external_process`）② 新工具 = manifest +（必要时）providers 扩展 ③ CI：`cargo test` + grep 断言 |

### 9.2 测试分层

| 层 | 覆盖 |
|----|------|
| 单元 | 版本点分比较器；兼容匹配规则（P0-C §2.2 → 表驱动用例）；鸣潮双键 upsert / JSON 改写（临时 SQLite fixture）；种子表 schema 校验；哈希工具 |
| 集成 | wiremock 模拟 HYP / Kuro 响应（真实结构 fixture，不打真网）；备份 → 修改 → 验证 → 回滚全链路（fixture db）；启动编排（进程 mock） |
| 契约 | IPC 返回类型 ↔ UI 期望（tauri-specta 生成 TS 绑定，单源） |
| 架构 | §9.1 CI 断言 |
| 手动真机 | 02 §4 全部 P0 Given/When/Then + 5 游戏物理环境 + §10 待实测项 |

### 9.3 真机验收

- 三组发布验收（02 §7）：系统级（00 §12.4 A）+ 功能级（02 §4 P0）+ 架构不变量
- **鸣潮 3.x 首测**为关键节点：验证双键写入 → 结果回写种子表（`Unknown → Verified` / 维持 `Unknown`）—— 兼容性数据生产机制的首次完整循环（00 §11.2）

---

## 10. 待实测清单（首批工程验证项）

> 继承 P0-B §5.5；每项结论回写本文档或 P0 记录增补，不阻塞其他模块开发。

| # | 项 | 方法 | 产出 |
|---|----|------|------|
| T1 | 米哈游国服 HYP base（`hyp-api.mihoyo.com` 推测待证）+ `getGamePackages` 国服 game_id | 抓包 / 社区交叉 | `HoyoPlayAdapter` region 表定稿 |
| T2 | 鸣潮国服索引常量差异（appId / appKey / gameId） | 同上 | `KuroAdapter` 常量表 |
| T3 | 鸣潮 3.x 双键写入 + db2 回写行为（是否需 `MenuData` / `PlayMenuInfo` 重置） | 真机（先备份沙盒） | 种子表 3.x 状态 + 写入器增强决策 |
| T4 | 原神解锁器编排（启动顺序 / CLI 参数 / 退出码语义） | 真机 | `LauncherOrchestrator` 编排定稿 |
| T5 | 5 游戏 exe 直启参数（米哈游服区参数等）+ `VERSIONINFO` 版本可得性 | 真机 | `LaunchSpec` / 版本识别方式定稿 |
| T6 | 鸣潮游戏目录写权限（`Program Files` 安装场景） | 真机 | 权限策略结论（§11 Q2） |
| T7 | 5 游戏注册表 / 发现路径实际键值 | 真机 | `DetectRuleSet` 定稿 |

---

## 11. 开放问题

| # | 问题 | 影响 | 倾向 |
|---|------|------|------|
| Q1 | 解锁器资产构建管道：基于上游源码自行构建（附 MIT 声明）vs 复用上游发行物再分发 | NOTICE 义务 / 资产哈希管理 | 倾向自行构建（可控 + 上游下架免疫），随 Orbis Releases 发资产 |
| Q2 | 写权限不足场景是否引入受限提权（UAC 单次） | B1 可用性边界 | MVP 仅阻止 + 引导；提权 P1 评估（00 §8.9 倾向最小权限） |
| Q3 | `tauri-specta` 引入与否（TS 绑定生成） | 开发效率 / 依赖面 | 倾向引入（MIT，消除手工双维护） |
| Q4 | 日志保留天数默认值（本文拟 14 天） | 磁盘占用 | 设置项暴露，默认 14 天 |
| Q5 | B 服（bilibili 服）发现规则与 region 映射 | A1 覆盖度 | 随 T1 / T7 一并实测；版本源复用国服 |

---

## 附录 A：模块 ↔ 功能覆盖矩阵

| 功能（01 清单） | 模块（本文） | 状态 |
|-----------------|--------------|------|
| A1–A3 发现 / 添加 / 识别 | GameScanner（DetectRule）+ catalog | 覆盖（T5 / T7 待实测） |
| A4–A5 启动 / 状态 | LauncherOrchestrator + ProcessMonitor | 覆盖 |
| A6 时长 | PlaytimeTracker | 覆盖 |
| A7 参数 | launch_profile | 覆盖 |
| A8 备份 / 恢复 | BackupManager + ConfigSource.declared_paths | 覆盖（鸣潮；其余「暂不支持」） |
| B1–B4 鸣潮 L1 链路 | EnhancementExecutor + WuwaConfigProvider | 覆盖（T3 待实测） |
| B5 历史与恢复 UI | BackupManager + S5 页面（03 §3） | 覆盖 |
| B6 兼容提示 | CompatibilityEngine | 覆盖 |
| B7 原神解锁 | UnlockerRunner（发行包分离） | 覆盖（T4 待实测） |
| B8 启动编排 | LauncherOrchestrator（skipUnlock） | 覆盖 |
| C1–C3 Manifest / 启停 / 风险 | ToolManifest + ToolRuntime | 覆盖 |
| C4 兼容数据 | seed.json + CompatibilityEngine | 覆盖 |
| D1–D4 UI | S0–S6（03 §3）+ IPC 命令面 + `tool:apply-step` | 覆盖（视觉见 03） |
| E1 更新检测 | VersionProvider ×3 | 覆盖（T1 / T2 待实测） |

结论：01 清单 P0 全覆盖，无架构缺口（与 01 §5 结论一致）。

## 附录 B：变更记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v0.1 | 2026-09-18 | 初版：技术栈定稿（Tauri 2 / Rust / React / SQLite）；进程拓扑与权限模型；Workspace 分层与依赖方向；11 个核心模块设计（trait 签名级）；SQLite schema（7 实体 MVP 子集）；L1 状态机与红线落地；L3 授权流与发行包分离；降级策略总表；架构不变量工程保障；待实测清单 T1–T7 |

> **本文档是原型开发的直接输入。** 范围争议回 01 清单评审，验收争议回 02 PRD，视觉与交互见 03 UI 设计文档；三者均不受本文档约束性突破。
